# SUPERSEDED — DO NOT USE

This brief was based on a reversed instruction.

Use this corrected first-run brief instead:

```text
docs/CODEX_MIMI_21_V2_CURATED_FIRST_RUN_BUILD.md
```

Correct current decision:

```text
For the first handwritten / typed / scrawled build, use:

><)))0>
<0(((><
```

Meaning:

- active 21_v2 first-run codes may use parenthesis-body fish for ease of handwriting/type.
- preserve the original brace-based full 104 archive as `legacy_code` / future source.
- do not treat the first-run parenthesis standard as permanent.

This file remains only as a warning against using the wrong direction.
